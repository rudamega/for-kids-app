<div class="row justify-content-center">
  <div class="col-6">
    <h1>Create new - Items</h1>
    <%= simple_form_for(@item) do |t| %>
      <%= t.input :name%>
      <%= t.input :description%>
      <%= t.input :price, label: "USD-Number only", placeholder: "Ex: 50.00"%>
      <%= t.submit%>
    <% end %>
    <%= link_to 'Back', items_path, class: "btn btn-outline-secondary" %>
  </div>
</div>
